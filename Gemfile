# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in svg_use.gemspec
gemspec

if ENV.has_key?("RAILS_VERSION")
  gem "actionview", ENV["RAILS_VERSION"]
  gem "activesupport", ENV["RAILS_VERSION"]
end
